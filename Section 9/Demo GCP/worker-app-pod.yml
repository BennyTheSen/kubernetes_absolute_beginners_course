apiVersion: v1
kind: Pod
metadata:
  name: voting-app-pod
  labels:
    name: worker-app-pod
    app: worker-voting-app
spec:
  containers:
  - name: worker-app
    image: dockersamples/examplevotingapp_worker
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
